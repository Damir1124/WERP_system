/**
 * Конфигурация порогов свежести заказов
 * 
 * TODO: В будущем эти значения будут настраиваться через админку
 * Endpoint: GET /api/config/order-freshness/
 */

export const ORDER_FRESHNESS_CONFIG = {
  // Через сколько минут заказ становится "жёлтым" (предупреждение)
  yellowAfterMinutes: 180,   // 3 часа
  
  // Через сколько минут заказ становится "красным" (критично)
  redAfterMinutes: 360,      // 6 часов
}
