{
  "dependencies": {
    "@twa-dev/sdk": "^8.0.2",
    "react-router-dom": "^7.15.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.14",
    "tailwindcss": "^4.3.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build:client": "cd client && npm install && npm run build",
    "build:courier": "cd courier && npm install && npm run build",
    "build": "npm-run-all build:client build:courier"
  }
}
