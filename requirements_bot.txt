# Зависимости для Telegram-бота (tg_bot)
aiogram==3.17.0
aiohttp==3.11.10
python-dotenv==1.0.1

# Для работы с Django API (если бот запускается внутри проекта, зависимости уже установлены)
# Эти зависимости уже есть в requirements.txt, но перечисляем для ясности
requests==2.32.3

# Для логирования (опционально)
colorlog==6.8.2

# Для работы с Redis (RedisStorage). Версия python-redis поступает из
# requirements.txt (redis==7.4.0), который также устанавливается в образ бота.
# НЕ задавай здесь redis — это конфликтует с requirements.txt.